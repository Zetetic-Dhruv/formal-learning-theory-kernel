# Artifact Checklist

All counts below are generated by `scripts/metrics.sh` and must match the README header table and `premise/final.json`.

## Toolchain

| Component | Version | Pinned? |
|-----------|---------|---------|
| Lean | `v4.29.0-rc6` | Yes (`lean-toolchain`) |
| Mathlib4 | `fde0cc508f` | Yes (`lakefile.lean` + `lake-manifest.json`) |
| Lake | bundled with Lean | Yes |

## Build

```bash
# Prerequisites: elan (https://github.com/leanprover/elan)
curl https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh -sSf | sh

# Build (first run fetches Mathlib)
lake build

# Subsequent builds with cached Mathlib: ~2 minutes (incremental)
```

**Expected build time (local, clean):** 15-25 minutes (dominated by Mathlib compilation).

**Expected build time (GitHub Actions, clean):** 60-90 minutes (slower runners, Mathlib fetch on cache miss).

**Expected build time (cached Mathlib):** 1-3 minutes locally, 5-10 minutes on CI.

**Expected output:** 0 errors.

## Sorry Inventory

Exactly 2 `sorry` tactics exist in the kernel. Both are blocked by published results absent from Mathlib.

| # | File | Theorem | Blocked by | Citation |
|---|------|---------|-----------|----------|
| 1 | `FLT_Proofs/Complexity/Generalization.lean:1903` | `vcdim_finite_imp_compression` | Approximate minimax on bounded-VC binary matrices | Moran-Yehudayoff 2016 (arXiv:1503.06960) |
| 2 | `FLT_Proofs/Theorem/Extended.lean:40` | `bhmz_middle_branch` | One-inclusion graph learners + doubling aggregation | Bousquet-Hanneke-Moran-Zhivotovskiy, STOC 2021 |

**CI enforcement:** The GitHub Actions workflow fails if the sorry count exceeds 2. The check matches only standalone `sorry` tactics (`^\s*sorry\s*$`), not comment mentions.

## Verification Commands

```bash
# Count sorrys (should print exactly 2 lines)
grep -rn --include="*.lean" -E '^[[:space:]]*sorry[[:space:]]*$' FLT_Proofs/

# Verify no new files with sorry
grep -rl --include="*.lean" -E '^[[:space:]]*sorry[[:space:]]*$' FLT_Proofs/
# Expected: Complexity/Generalization.lean, Theorem/Extended.lean

# Generate canonical metrics (should match README and premise/final.json)
./scripts/metrics.sh

# Full build from clean state
rm -rf .lake/build
lake build
```

## Default Target

The default Lake target is `FLT_Proofs` (all 53 files). A second target `WorldModel` builds the proof operad (8 files). Both permit `sorry` (Lean's default). The CI workflow enforces the sorry ceiling independently.

## File Inventory

All counts generated by `scripts/metrics.sh`:

- 53 `.lean` files in `FLT_Proofs/`
- 21,522 lines of Lean4
- 354 theorem/lemma statements (248 public, 106 private)
- 257 definitions
- 59 structures
- 0 sorry tactics

World model (`world_model/WorldModel/`): 1,170 lines, 0 sorry, 27/27 tests pass.
